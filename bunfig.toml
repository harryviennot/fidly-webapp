[test]
# Runs before any test module loads. Sets a default NEXT_PUBLIC_API_URL so
# tests that import @/api/client (via the wizard component tree) don't throw
# under NODE_ENV=test, where Bun skips .env.local. See src/test-setup.ts.
preload = ["./src/test-setup.ts"]
